import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sirat.ai',
  appName: 'sirat-ai',
  webDir: 'out'
  // Server block removed to load embedded lightning fast assets!
};

export default config;